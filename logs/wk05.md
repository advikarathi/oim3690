# Week 5 Log

Week: 06/15 - 06/21

> **What this is**: a short weekly summary of what you built, learned, and how you used AI. Not a diary — just structured evidence of your work.
>
> **Format**: use proper Markdown — headings, bullet points, code blocks. This file will be rendered on GitHub. If it looks like plain text with no formatting, you're doing it wrong.
>
> **Required**: all 4 sections present and non-empty. Empty sections = incomplete log.

## What I worked on

- Downloaded `js-demo.html` from the course site and dropped it into my `oim3690` repo
- Opened it with Live Server and clicked through all 5 things — the color changer, mailbox, time button, hide/seek toggle, and the surprise button (yes I fell for it)
- Went back through the code after to actually understand what was happening, not just watch it work

## What I learned

### JS can grab HTML elements and mess with them
- `document.querySelector("#id")` and `document.getElementById("id")` both let you pull an element out of the page so you can do stuff to it
- once you have it, you can change styles, text, classes — basically anything

### Event listeners
- instead of putting `onclick=` directly in the HTML, you can attach behavior in the JS with `.addEventListener()` — feels cleaner, keeps things separated
- didn't realize you could listen for `"mouseover"` too, not just clicks — that's what makes the mailbox grow when you hover

### How the random color thing works
- there's an array of hex colors and it picks one randomly using `Math.floor(Math.random() * n)` — `Math.random()` gives you a decimal between 0 and 1, then `Math.floor` rounds it down so it lands on a real index
- makes sense once I saw it written out

### The hide/seek toggle uses CSS, not JS directly
- the button doesn't actually hide the text itself — it just adds or removes a class called `hidden`, and the CSS is what does `.hidden { display: none; }`
- kind of a weird split of responsibility but I get why

### `parseInt` with a fallback
- the mailbox font size is read with `parseInt(mailbox.style.fontSize) || 30` — the `|| 30` is there because if the size hasn't been set yet, `parseInt` would return `NaN` and the whole thing would break

## AI interactions

### Interaction 1

- **What I asked**: asked Claude to help me draft this log using the js-demo.html file and the assignment prompt
- **What AI gave me**: a full draft with all four sections filled out, code snippets, and questions based on the actual code
- **What I kept / changed / rejected**: rewrote most of the wording so it didn't sound like a robot wrote it, kept the structure and the specific questions since those were actually things I was confused about

## Questions

- **why does `DOMContentLoaded` matter here?** the script is already at the bottom of the body so everything should be loaded — is wrapping the console logs in that listener actually doing anything, or is it just habit?

- **what's the difference between `querySelector` and `getElementById`?** the demo uses both and they seem identical — is there a reason to prefer one?

- **what does `"_blank"` mean in `window.open(url, "_blank")`?** guessing it's "open in new tab" but what are the other options?

- **why does the mailbox reset to 30px instead of just stopping at 200?** `if (size > 200) size = 30` loops it back — was curious if there's a reason for that vs just capping it

- **what are `role="img"` and `aria-label` on the mailbox span?** never seen those before — are they always needed for emojis or just good practice?
