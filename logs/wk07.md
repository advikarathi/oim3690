# Week 07 Log

Week: 06/29 - 07/02

## What I worked on

- Started MP3, the API-powered app project, and chose TheMealDB for a recipe explorer concept: "Eat Your Way Around the World," browsing meals by country/cuisine with a save-favorites feature.
- Looked at the live JSON for TheMealDB's `list.php`, `filter.php`, and `lookup.php` endpoints before committing to anything, per the assignment's advice.
- Wrote and committed `PROPOSAL.md` covering the app concept, API choice, core features, and open questions.
- Built Iteration 1: a basic HTML page, a `fetch()` call hard-coded to one cuisine (`Canadian`), `console.log`-ed the raw response, and rendered the meal results as cards on the page.
- Built the two fetch practice pages from the 07/02 session: `fetch-fun.html` (fetch a free public API — dog images, programming jokes, and advice — show a field, `try`/`catch`) and `post-message.html` (POST a message to the class board with my own `X-Token`).

## Class notes (07/02)

Finished the MP2 Showcase and covered how to pull live data from an API with `fetch()`.

- Full recap with code examples is on the session page; the Session 13 recording is posted too.
- MP2 Class Favorite vote and the lightning talks we didn't reach today are Monday, along with how to keep an API key safe and how to start organizing code with browser APIs.

## Before Monday (07/06)

- [x] `fetch-fun.html` and `post-message.html` in this repo.
- [ ] MP3 proposal: create a new public repo, pick an API, open its URL in the browser and read the raw JSON, then write `PROPOSAL.md`. **Due Sunday 7/5.** Final MP3 due Sunday 7/12.
- [ ] Finish `playlist.html` from last class.
- [x] Keep this weekly log current.

## What I learned

- Arrays hold many values in one variable, indexed from zero — `fruits[0]` is the first item, not `fruits[1]`.
- Three loop types cover most needs: `for...of` when you just need each value, the classic `for` when you specifically need the index, and `forEach` when you want both at once — useful for things like wiring up the same click handler across several elements.
- The pattern behind most real apps — a feed, a search box, a gallery — is just an array, a loop, and an `if`. Once that clicks, rendering a list of API results is the same pattern as anything else that loops over data.
- A loop needs a correct start, stop, and step — we saw a bad one freeze the browser tab live in class, which was a good reminder to double-check loop conditions before running anything, especially AI-written ones.
- `fetch()` only resolves with a `Response` object, not usable data — you need a second `await response.json()` before there's anything to actually loop over:
  ```js
  const response = await fetch(API_URL);
  const data = await response.json(); // now data.meals is usable
  ```
- TheMealDB's `filter.php?a=<area>` only returns summary fields (`idMeal`, `strMeal`, `strMealThumb`, `strArea`, `strCountry`) — getting full ingredients and instructions for a recipe needs a second call to `lookup.php?i=<id>`.
- The area list (`list.php?a=list`) now covers 195+ countries, but the actual recipe database is much smaller (IDs only run into the low 53,000s), so a lot of those areas will return zero meals — that's a real case to design around, not a bug.

## AI interactions

### Interaction 1

- **What I asked**: For help getting started on MP3 — brainstorming app/API ideas tied to things I'm actually into, then more options when the first batch didn't land.
- **What AI gave me**: A shortlist (countries explorer, chess stats app, art gallery browser), then a second round (crypto watchlist, weather/packing planner, recipe-by-cuisine app, astronomy calendar) with reasoning for each.
- **What I kept / changed / rejected**: Picked the recipe-by-cuisine idea — filtering TheMealDB by country with a favorites feature — over the rest.

### Interaction 2

- **What I asked**: To check the real API data before proposing, then draft `PROPOSAL.md`, then build out Iteration 1 once the repo was live.
- **What AI gave me**: A live-checked rundown of the three endpoints I'd need (including the area-list size finding above), a complete `PROPOSAL.md` draft, and the `index.html` / `style.css` / `script.js` files for the hard-coded fetch-and-display version.
- **What I kept / changed / rejected**: Used the proposal mostly as-is, rewriting the "why I chose this" section in my own words; used the Iteration 1 code as the working starting point in the repo.

## Questions

- How should I handle all the countries/cuisines that come back with zero meals — filter them out of the dropdown ahead of time, or just show a friendly empty-state message when it happens?
- What's the cleanest way to chain the filter call and a second lookup call for a detail view, without the code turning into a mess of nested `.then()`s?
- For favorites, is it better to store full meal objects in `localStorage`, or just the IDs and re-fetch details when needed?
